<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.steammanager.myapps.mapper.MyappsMapper">
		
		<insert id="insertFollow">  
			insert ignore into follow(user_id, app_id) values(#{userid},#{appid})
		</insert>  

	</mapper>
